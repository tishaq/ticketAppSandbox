<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ajisaq.ticketingappsanbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".TransactionSummary"></activity>
        <activity android:name=".LoadingSetting" />
        <activity android:name=".GatePassSetting" />
        <activity android:name=".AnimalsSetting" />
        <activity android:name=".ProduceSetting" />
        <activity android:name=".Summary" />
        <activity android:name=".CategorySetting" />
        <activity android:name=".DeviceSetting" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AdminLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>